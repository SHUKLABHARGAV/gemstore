import 'package:flutter/material.dart';

class password_change_popup extends StatelessWidget {
  const password_change_popup({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('password_change_popup'),
      ),
      body: Center(
        child: Text('password_change_popup'),
      ),
    );
  }
}
